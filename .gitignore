.env
venv

.env.*
!.env.example

__pycache__/
*.pyc

# Google Drive OAuth: the downloaded client secret and the token this app
# caches after the one-time browser consent. Both are per-machine secrets,
# same reasoning as .env above.
credentials/